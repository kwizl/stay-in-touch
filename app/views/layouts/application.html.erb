<!DOCTYPE html>
<html>
  <head>
    <title>Stay in Touch</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta property='og:title' content='Stay In Touch'/>
    <meta property='og:image' content='https://social-media.herokuapp.com/app/assets/images/app_screenshot.png'/>
    <meta property='og:description' content='Stay In Touch is a social media web application. The application enables registration of users, Sign In of users, posting comments, liking posts, and sending friend requests.'/>
    <meta property='og:url' content='https://social-media.herokuapp.com'/>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <nav class="nav">
      <%= link_to 'Stay in touch', root_path %>
      <div class="menu">
        <div class="menu-item">
          <%= menu_link_to 'Timeline', posts_path %>
        </div>
        <div class="menu-item">
          <%= menu_link_to 'All users', users_path %>
        </div>
        <div class="menu-item">
          <%= menu_link_to 'Invitations', user_invitations_path %>
        </div>
        <div class="menu-item">
          <%= menu_link_to 'Friends', friendships_path %>
        </div>
      </div>
      <% if current_user %>
        <%= link_to current_user.name, current_user %>
        <%= link_to 'Sign out', destroy_user_session_path, method: :delete %>
      <% else %>
        <%= link_to 'Sign in', user_session_path %>
      <% end %>
    </nav>
    <% if notice.present? %>
      <div class="notice">
        <p> <%= notice %> </p>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="alert">
        <p> <%= alert %> </p>
      </div>
    <% end %>
    <main class="main">
      <%= yield %>
    </main>
  </body>
</html>
